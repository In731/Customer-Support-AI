import AdminClient from "@/components/AdminClient";
import React from "react";

export const metadata = {
    title: "NexSupport AI — Operations Super Admin",
    description: "Internal team operations and platform metrics for NexSupport AI",
};

export default function AdminPage() {
    return <AdminClient />;
}
